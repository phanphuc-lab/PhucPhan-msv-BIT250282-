class person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    @classmethod
    def from_string(cls,text):
        name, age = text.split("-")
        return cls(name,int(age))
p = person.from_string("Hoang-101")
print(p.name)
print(p.age)
